# AGENTS.md

## Commands

- Run `python main.py` to execute the basic add function (manual testing only, no test suite).

## Notes

- This is a minimal learning repository with no build system, tests, or complex tooling.
- `main.py` contains only a simple `add(a, b)` function.